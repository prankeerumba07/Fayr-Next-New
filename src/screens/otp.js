// otp — fayr-design.browser.jsx
//
// Typing the six digit code. The paused and restricted states are now screens of their own, at otplocked and blocked.
//
// The screen itself lives at ../firstrun/OtpScreen and is unchanged. This file
// exists so that every design screen has one file under its own key, which is
// what src/screens/keys.js claims and keys.test.mjs checks. There is exactly one
// implementation: nothing is copied here.
export { default } from '../firstrun/OtpScreen';
