// splash — fayr-design.browser.jsx
//
// The logo, while the app wakes up and works out where to send you.
//
// The screen itself lives at ../firstrun/SplashScreen and is unchanged. This file
// exists so that every design screen has one file under its own key, which is
// what src/screens/keys.js claims and keys.test.mjs checks. There is exactly one
// implementation: nothing is copied here.
export { default } from '../firstrun/SplashScreen';
