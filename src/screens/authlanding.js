// authlanding — fayr-design.browser.jsx
//
// Shop. Review. Earn. The choice of how to sign in.
//
// The screen itself lives at ../firstrun/AuthLandingScreen and is unchanged. This file
// exists so that every design screen has one file under its own key, which is
// what src/screens/keys.js claims and keys.test.mjs checks. There is exactly one
// implementation: nothing is copied here.
export { default } from '../firstrun/AuthLandingScreen';
