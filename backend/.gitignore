# dependencies
node_modules

# build output
dist
*.tsbuildinfo

# env / secrets — never commit real values
.env
.env.*
!.env.example

# logs
*.log
logs

# operator uploads (campaign images, etc.) — user content, never committed
uploads

# os / editor
.DS_Store
